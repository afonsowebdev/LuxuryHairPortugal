import type { ContactMessage, NewsletterSubscriber } from "@/types";

/**
 * Sem mensagens/subscritores de exemplo — populados pelas submissões reais
 * do formulário de contacto e da newsletter (ver AdminDataContext).
 */
export const contactMessages: ContactMessage[] = [];
export const newsletterSubscribers: NewsletterSubscriber[] = [];
